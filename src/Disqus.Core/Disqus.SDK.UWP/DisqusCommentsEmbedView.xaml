<UserControl
    x:Class="Disqus.SDK.UWP.DisqusCommentsEmbedView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Disqus.SDK.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <ProgressBar 
            x:Name="LoadingProgressBar"
            IsIndeterminate="False" 
            VerticalAlignment="Top"
            />
        <WebView
            x:Name="CommentsWebView"
            DOMContentLoaded="CommentsWebView_DOMContentLoaded"
            NavigationStarting="CommentsWebView_NavigationStarting"
            NavigationCompleted="CommentsWebView_NavigationCompleted"
            NewWindowRequested="CommentsWebView_NewWindowRequested"
            ScriptNotify="CommentsWebView_ScriptNotify"
            />
    </Grid>
</UserControl>
